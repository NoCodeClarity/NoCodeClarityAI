[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "bun run packages/orchestrator/src/server.ts"
  healthcheckPath = "/health"
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3

[[services]]
  name = "orchestrator"
  internalPort = 3001
